<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <supports-screens
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true"
        android:anyDensity="true" />

    <application
        android:name="org.tjc.bible.BibleApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.NoActionBar">
        <activity
            android:exported="true"
            android:name="org.tjc.bible.MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|uiMode|smallestScreenSize|density"
            android:windowSoftInputMode="adjustResize"
            android:resizeableActivity="true"
            android:supportsPictureInPicture="true">
            <layout android:defaultHeight="720dp"
                    android:defaultWidth="1024dp"
                    android:minHeight="480dp"
                    android:minWidth="400dp" />

            <property
                android:name="android.window.PROPERTY_ACTIVITY_EMBEDDING_SPLIT_ENABLED"
                android:value="true" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
